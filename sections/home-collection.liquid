<section class="card__collection">
  <div class="card__collection--width">
    <h1>{{ collections.frontpage.title }}</h1>
    <ul class="card__container">
      {% for product in collections.frontpage.products limit: section.settings.products_to_show %}
        <a class="card__link" href="{{ product.url }}">
          {% render 'product-card', product: product %}
        </a>
      {% endfor %}
    </ul>
  </div>
</section>

{% schema %}
{
  "name": "home-collection",
  "settings": [
    {
      "type": "range",
      "id": "products_to_show",
      "min": 2,
      "max": 25,
      "step": 1,
      "default": 8,
      "label": "Products to show"
    }
  ]
}
{% endschema %}
